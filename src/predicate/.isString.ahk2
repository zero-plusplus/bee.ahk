/**
 * Determines whether the `value` is an string or not.
 * @method
 * @param {any} value
 * @return {boolean}
 */
class isString extends bee.rule.PredicateFunc {
  static behavior(value) {
    return value is String
  }
}