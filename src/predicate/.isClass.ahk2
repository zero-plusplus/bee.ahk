/**
 * Determines whether the `value` is an class or not.
 * @method
 * @param {any} value
 * @return {boolean}
 */
class isClass extends bee.rule.PredicateFunc {
  static behavior(value) {
    return value is Class
  }
}