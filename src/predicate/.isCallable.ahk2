/**
 * Determines whether the `value` has a call method or not.
 * @method
 * @param {any} value
 * @return {boolean}
 */
class isCallable extends bee.rule.PredicateFunc {
  static behavior(value) {
    return bee.isFunc(value) || bee.isFunctor(value)
  }
}