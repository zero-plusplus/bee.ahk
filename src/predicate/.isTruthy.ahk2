/**
 * Determines whether the `value` is an truthy value or not.
 * @method
 * @param {any} value
 * @return {boolean}
 */
class isTruthy extends bee.rule.PredicateFunc {
  static behavior(value) {
    return !!value == true
  }
}