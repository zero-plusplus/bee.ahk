/**
 * Determines whether the `value` is an 0 to 9 or not.
 * @method
 * @param {any} value
 * @return {boolean}
 */
class isDigit extends bee.rule.PredicateFunc {
  static behavior(value) {
    if (!bee.isNumber(value)) {
      return false
    }
    return 0 < value ~= "^[0-9]$"
  }
}